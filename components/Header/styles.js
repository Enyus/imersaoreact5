import styled from "styled-components";

const StyledHeader = styled.header`
    .banner {
        width: 100%;
        height: 230px;
        object-fit: cover;
    }
`;

export default StyledHeader;